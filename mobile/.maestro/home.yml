appId: com.layerzwallet.mobile
---
- launchApp:
    clearState: true
    clearKeychain: true

# Local iOS test fails without this. Race condition?
- waitForAnimationToEnd
- assertVisible: 'Welcome to LZW'
